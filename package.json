{
  "name": "loginzz",
  "version": "1.0.0",
  "description": "Telegram Login implementation for web applications",
  "main": "app/src/server.js",
  "type": "module",
  "scripts": {
    "start": "node app/src/server.js",
    "dev": "nodemon app/src/server.js",
    "build": "mkdir -p .vercel/output/static && cp -r app/public/* .vercel/output/static/",
    "vercel-build": "mkdir -p .vercel/output/static && cp -r app/public/* .vercel/output/static/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "telegram",
    "login",
    "authentication",
    "express",
    "oauth"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
